{
  "name": "yin-yang-grocery-planner",
  "version": "1.0.0",
  "description": "Weekly grocery planning full-stack app",
  "private": true,
  "scripts": {
    "dev": "npm run dev:free-port && concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:free-port": "lsof -ti:3001 | xargs kill -9 2>/dev/null || true",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "npm install && cd backend && npm install && npm run init-db && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
